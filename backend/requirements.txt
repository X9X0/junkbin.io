# Junkbin.io Backend Dependencies
#
# Pinned to exact versions (not >=) for reproducible installs - what's
# listed here is what's actually running and tested, not whatever the
# latest compatible release happens to be on a given day. Bumping a
# version is a deliberate, tested change, not a side effect of a fresh
# `pip install`. CI runs pip-audit against this file on every push to
# catch known vulnerabilities in pinned versions - bump the affected
# package(s) when it flags something, don't just widen the constraint.

# ========================================
# Core Django
# ========================================
Django==6.1
djangorestframework==3.18.0
django-environ==0.14.0
django-cors-headers==4.9.0

# ========================================
# Database
# ========================================
psycopg2-binary==2.9.12
dj-database-url==3.1.2

# ========================================
# Authentication & Authorization
# ========================================
django-allauth[socialaccount]==65.19.1
djangorestframework-simplejwt==5.5.1
PyJWT==2.13.0
requests==2.34.2
cryptography==50.0.1
google-auth==2.57.0

# ========================================
# API & Documentation
# ========================================
drf-spectacular==0.30.0
django-filter==25.1

# ========================================
# Async Task Queue
# ========================================
celery==5.6.3
redis==8.1.0
django-celery-beat==2.9.0
django-celery-results==2.6.0

# ========================================
# Web Push notifications
# ========================================
pywebpush==2.4.0

# ========================================
# Image Processing
# ========================================
Pillow==12.3.0
pillow-avif-plugin==1.6.0
django-imagekit==6.1.0

# Self-hosted background removal (no external API) - see apps/media_tools
rembg[cpu]==2.0.81
pymatting==1.1.15

# ========================================
# File Storage
# ========================================
django-storages==1.14.6
boto3==1.43.82

# ========================================
# Security
# ========================================
django-ratelimit==4.1.0
django-csp==4.0
django-permissions-policy==4.31.0
python-magic==0.4.27
django-axes==8.3.1

# ========================================
# Spreadsheet / BOM Import
# ========================================
xlrd==2.0.2
openpyxl==3.1.5
django-import-export==4.4.1
pdfplumber==0.11.10
pytesseract==0.3.13
pdf2image==1.17.0

# ========================================
# Utilities
# ========================================
python-slugify==8.0.4
python-dateutil==2.9.0.post0
pytz==2026.3.post1

# ========================================
# Server
# ========================================
gunicorn==26.2.0
whitenoise==6.12.0

# ========================================
# Testing
# ========================================
pytest==9.1.1
pytest-django==4.14.0
pytest-cov==7.1.0
factory-boy==3.3.3
faker==40.37.0

# ========================================
# Code Quality
# ========================================
black==26.5.1
flake8==7.3.0
isort==9.0.1
pylint==4.0.8
pylint-django==2.8.0

# ========================================
# Monitoring & Debugging (Optional)
# ========================================
django-debug-toolbar==7.1.1
sentry-sdk==2.68.1
psutil==7.2.2
django-prometheus==2.5.0
